/**
 * Converts a string to lowercase.
 * @param  {String} inputString the input string
 * @return {String}             the lowercased string
 */
export default function sanitizeToLowerCase (s) {
  return s.toLowerCase();
}
